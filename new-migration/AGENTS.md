# New Migration Workspace

The active migrated app is:

```text
roots-react-workbench/
```

Use this area for React/Vite Roots Workbench work, Vercel preparation, staging, and production changes. Do not reach back into `../legacy-static/` at runtime. If a legacy behavior or asset is needed, copy/adapt it intentionally into `roots-react-workbench/`.
