# Legacy Static Archive

This folder contains the old numerical analysis workspace for reference and fallback.
It is not the current Vercel deployment target.

The current migrated app lives at:

```text
../new-migration/roots-react-workbench/
```

Use this folder only for legacy-specific tasks or when porting a known piece of behavior, engine logic, or visual treatment into the migrated React app.
